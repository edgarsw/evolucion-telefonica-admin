networks:
  proxy:
    external: true

services:
  angular:
    build:
      context: ./
      dockerfile: Dockerfile
    container_name: neixt-evolution-profe
    restart: always
    networks:
      - proxy
    expose:
      - "4000" 